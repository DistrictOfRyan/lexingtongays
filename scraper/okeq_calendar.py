"""STUB — Tulsa-specific scraper for OKEQ's WordPress + Simple Calendar plugin.
Lexington's anchor org (Lexington Pride Center) uses a different platform.

When operator implements the Lexington Pride Center events scraper, this file can
either be deleted or repurposed. See TulsaGays' okeq_calendar.py for the
WordPress + simcal reference implementation if Lex ever uses that plugin.
"""

from typing import List, Dict


def scrape() -> List[Dict]:
    """Return [] — Lexington has no equivalent of OKEQ's calendar plugin."""
    return []
