"""STUB — Tulsa-specific scraper. Lexington's equivalent is the Lexington Pride Center
(see config.SOURCES['lex_pride_center']). The PCSO website at lexpridecenter.org has a
different structure than OKEQ's WordPress + Simple Calendar.

When operator wants Lex Pride Center events scraped automatically, replace this stub
with a fetcher for lexpridecenter.org's events page. See TulsaGays' okeq.py for the
reference implementation pattern.
"""

from typing import List, Dict


def scrape() -> List[Dict]:
    """Return [] until a Lex Pride Center scraper is written."""
    return []
